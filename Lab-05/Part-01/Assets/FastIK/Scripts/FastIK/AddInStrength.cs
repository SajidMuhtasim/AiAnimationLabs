//I was running into errors while modifying the code to add in weight strenght and couldnt figure out how to fix, 
//so I am adding bits of it here


// [Range(0, 1)]
//public float IKWeight = 1f;


/*  
    var finalPosition = Vector3.Lerp(targetPosition, Positions[Positions.Length - 1], IKWeight);
     SetPositionRootSpace(Bones[i], Vector3.Lerp(GetPositionRootSpace(Bones[i]), finalPosition, IKWeight));
*/